<div class='d-flex justify-content-center align-items-center'>
    <div class='container border border-light rounded py-5 px-3 home-page d-flex flex-column align-items-center justify-content-around'>
        <div class='name-container d-flex align-items-center justify-content-center'>
            <h1 class='app-name' >budgetapp</h1>
        </div>
        <div class="mt-auto w-100 text-center mb-3">
            <% unless user_signed_in? %>
                <%= link_to 'LOG IN', new_user_session_path, class: 'list-group-item list-group-item-action active' %> 
                <%= link_to 'SIGN UP', new_user_registration_path, class: 'list-group-item list-group-item-action' %>   
            <% end %>
        </div>
    </div>
</div>